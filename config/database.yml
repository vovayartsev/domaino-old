defaults: &defaults
  adapter: postgresql
  encoding: unicode
  pool: 5
  username: postgres
  password: postgres

development:
  <<: *defaults
  host: localhost
  database: domaino_development

test:
  <<: *defaults
  database: domaino_test

production:
  <<: *defaults
  database: domaino_production

