javascript:
  $.fn.api.settings.beforeXHR = function (xhr) {
    xhr.setRequestHeader('X-CSRF-Token', $('meta[name=csrf-token]').attr('content'));
  };

  $.fn.api.settings.method = 'POST';

  $.fn.api.settings.api = {
    'test alert': "#{test_alerts_path}"
  };